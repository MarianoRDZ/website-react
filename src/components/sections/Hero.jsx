import HeroSection from './HeroSection';

const Hero = () => {
  return (
    <section className="flex items-center py-4">
      <HeroSection imageSize="lg" />
    </section>
  );
};

export default Hero;
